{
  "name": "ballerina-salesforce-mcp-server",
  "version": "1.1.0",
  "description": "AI-driven MCP server that scaffolds, configures, and deploys end-to-end Ballerina + Salesforce integrations in a WSO2 Integrator (BI) workspace — 18 tools, from OAuth to a running REST service.",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "setup": "node scripts/setup.mjs",
    "refresh-token": "node scripts/setup.mjs --refresh-only",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "axios": "^1.7.7",
    "express": "^4.21.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.7.5",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
